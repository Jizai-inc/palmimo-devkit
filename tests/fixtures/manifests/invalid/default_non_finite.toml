# default is not finite: TOML's float grammar allows inf/nan literals, but
# a param default that is not a finite number is not a usable value.
schema = 1
name = "bad-app"
description = "test"
command = ["run", "--rate", "{rate}"]

[params.rate]
type = "float"
default = inf
